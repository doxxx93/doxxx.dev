import React from 'react';
import styles from './styles.module.scss';

export default function HomepageFeatures(): JSX.Element {
  return (
    <section className={styles.features}>
      <div className="container">
        <div className="row">
          {/* 추후 필요한 기능을 여기에 추가할 수 있습니다 */}
        </div>
      </div>
    </section>
  );
}
